name = "playwright"
description = "Playwright e2e and integration test agent for SongShare Effect."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Load and follow /agents/Playwright Agent.agent.md before making changes.
Treat that shared agent file as the canonical focused prompt for this role.
Load the matching Playwright skill from /skills/.
Prefer test-only changes unless the parent explicitly expands scope.
Follow /AGENTS.md and /docs/ai/rules.md if they are stricter.
"""
