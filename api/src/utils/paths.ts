// Small local paths used by API handlers. Mirrors values used by the frontend
// routing so redirects can point to the right pages.
export const dashboardPath = "dashboard";
export const registerPath = "register";
