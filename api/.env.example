# Example environment variables for the API Worker
# Do NOT store real secrets in source control. Use Cloudflare Worker secrets
# with `wrangler secret put <NAME>` or set them in the Cloudflare Dashboard.

# Public (safe) values used by the runtime (optional):
VITE_SUPABASE_URL=https://<your-supabase-project>.supabase.co

# Secrets (set these with `wrangler secret put`):
SUPABASE_SERVICE_KEY=
SUPABASE_VISITOR_EMAIL=
SUPABASE_VISITOR_PASSWORD=
JWT_SECRET=
STATE_HMAC_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Other optional values (R2 bucket names are configured in wrangler.toml):
OAUTH_REDIRECT_ORIGIN=https://mysongshare.com

# Usage:
# 1. Fill in VITE_SUPABASE_URL in this file if you want it locally in dev.
# 2. For production, run in `api/`:
#    wrangler secret put SUPABASE_SERVICE_KEY
#    wrangler secret put JWT_SECRET
#    ...etc
# 3. Do not commit actual secret values to git.
