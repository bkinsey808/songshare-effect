/**
 * Test helper for extractValidationErrors - provides values that trigger edge paths.
 * Use makeNull from @/shared/test-utils/makeNull.test-util for null input.
 */

const INVALID_PRIMITIVE = 42;
export default INVALID_PRIMITIVE;
