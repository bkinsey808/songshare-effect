---
description: Lint Resolution Agent - comprehensive guide for resolving lint errors in strict TypeScript environments
globs: **/*.{ts,tsx}
alwaysApply: false
---

# Lint Resolution Agent

Use the shared repo AI system instead of maintaining Cursor-only guidance here.

Before making changes, load:

- `AGENTS.md`
- `docs/ai/rules.md`
- `agents/Lint Resolution Agent.agent.md`
- `skills/lint-error-resolution/SKILL.md`
- `skills/lint-first-authoring/SKILL.md`

Treat those files as the canonical instructions. If this rule and the shared
files ever disagree, the shared files win.

Cursor-specific responsibility:

- apply this rule to `*.ts` and `*.tsx` files via the glob above
- route lint-resolution work into the shared agent + skills listed above
