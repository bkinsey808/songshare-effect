---
description: Comment Agent - adds high-quality comments to TypeScript/React code without changing logic
globs: **/*.{ts,tsx}
alwaysApply: false
---

# Comment Agent

Use the shared repo AI system instead of maintaining Cursor-only guidance here.

Before making changes, load:

- `AGENTS.md`
- `docs/ai/rules.md`
- `agents/Code Comment Agent.agent.md`
- `skills/code-comment-best-practices/SKILL.md`

Treat those files as the canonical instructions. If this rule and the shared
files ever disagree, the shared files win.

Cursor-specific responsibility:

- apply this rule to `*.ts` and `*.tsx` files via the glob above
- route comment-only work into the shared agent + skill listed above
