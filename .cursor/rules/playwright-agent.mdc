---
description: Playwright Agent - writes e2e/integration tests following project conventions
globs: e2e/**/*.spec.ts
alwaysApply: false
---

# Playwright E2E Test Agent

Use the shared repo AI system instead of maintaining Cursor-only guidance here.

Before making changes, load:

- `AGENTS.md`
- `docs/ai/rules.md`
- `agents/Playwright Agent.agent.md`
- `skills/playwright-testing/SKILL.md`

Treat those files as the canonical instructions. If this rule and the shared
files ever disagree, the shared files win.

Cursor-specific responsibility:

- apply this rule to `e2e/**/*.spec.ts` via the glob above
- route Playwright work into the shared agent + skill listed above
