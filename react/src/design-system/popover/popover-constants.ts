// Shared numeric constants for popover layout calculations

/** Minimum margin to keep popover off viewport edges */
export const MIN_MARGIN = 8;

/** Default gap between trigger and popover when placed */
export const GAP_DEFAULT = 8;

/** Divisor used for centering calculations (half width/height) */
export const CENTER_DIVISOR = 2;
