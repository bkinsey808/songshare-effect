import SongForm from "@/react/song/song-form/SongForm";

/**
 * Page that renders the song edit form.
 *
 * @returns ReactElement mounting the `SongForm` component
 */
function SongEditPage(): ReactElement {
	return <SongForm />;
}

export default SongEditPage;
