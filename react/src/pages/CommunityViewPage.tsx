import CommunityView from "../community/view/CommunityView";

/**
 * Page wrapper that renders the `CommunityView` component.
 *
 * @returns ReactElement mounting the community view
 */
export default function CommunityViewPage(): ReactElement {
	return <CommunityView />;
}
